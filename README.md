# EasySQL SDK — TypeScript

Official TypeScript SDK for the [EasySQL API](https://easysql.net).

> ⚠️ **Auto-generated files.** `src/api-types.ts`, `src/client.ts`, and `docs/API.md`
> are auto-generated by the [`generate-sdk.yml`](./.github/workflows/generate-sdk.yml)
> workflow. Do not edit them by hand.

## Installation

```bash
npm install @clearsoft/easysql-sdk
```

## Usage

```typescript
import { createEasySQLClient } from "@clearsoft/easysql-sdk";

const api = createEasySQLClient({
  baseUrl: "https://api.easysql.net",
  accessToken: "your-access-token",
});
```

### Authentication

```typescript
// Login
const anon = createEasySQLClient({ baseUrl: "https://api.easysql.net" });

const { data: token, error } = await anon.login({
  email: "user@example.com",
  password: "my-password",
});

if (error) throw new Error(`Login failed: ${error}`);

// Authenticated client
const api = createEasySQLClient({
  baseUrl: "https://api.easysql.net",
  accessToken: token.access_token,
});

// Get current user
const { data: user } = await api.me();
```

### Running queries

```typescript
// Send a natural language query
const { data: result } = await api.createQuery({
  connector_id: "conn_abc123",
  question: "How many users signed up this month?",
});

// List recent queries
const { data: history } = await api.listQueries({ page: 1, per_page: 10 });
```

### Managing connectors

```typescript
// Create a connector
const { data: connector } = await api.createConnector({
  name: "Production DB",
  type: "mysql",
  config: {
    host: "db.example.com",
    port: 3306,
    database: "myapp",
    user: "readonly",
    password: "secret",
  },
});

// List connectors
const { data: connectors } = await api.listConnectors();

// Get a single connector
const { data: conn } = await api.getConnector({ connector_id: "abc-123" });

// Update a connector
const { data: updated } = await api.updateConnector(
  { name: "Staging DB" },
  { path: { connector_id: "abc-123" } },
);

// Delete a connector
await api.deleteConnector({ connector_id: "abc-123" });
```

### Dashboard stats

```typescript
const { data: stats } = await api.dashboardStats();
```

## API Reference

All endpoints are available as named methods on the client. Full reference: [`docs/API.md`](docs/API.md).

| Module | Methods |
|---|---|
| Auth | `register`, `login`, `refresh`, `me`, `deleteMe`, `updateMe`, `changePassword` |
| Billing | `getPlan`, `checkout`, `portal` |
| Connectors | `listConnectors`, `createConnector`, `testConnector`, `getConnector`, `updateConnector`, `deleteConnector`, `syncConnector` |
| Dashboard | `dashboardStats` |
| Queries | `listQueries`, `createQuery`, `getQuery` |
| Health | `health`, `healthV1` |

## Development

```bash
cp .env.example .env                 # configure API URL
make install                          # install dependencies
make generate                         # download spec → api-types.ts + client.ts + docs/API.md
make typecheck                        # check TypeScript types
make build                            # compile to dist/
make test                             # run unit tests
```

Or use `make all` for the full pipeline.

> Full API reference: [`docs/API.md`](docs/API.md)

## License

MIT
